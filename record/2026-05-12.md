---
questions: 1
source: "[[me]]"
---
## 1. 手撕NMS

``` python
import numpy as np

def nms(preds, iou_thr):
    scores = preds[:, 4]
    # 按 score 降序排序
    order = scores.argsort()[::-1]

    keep = []
    while len(order) > 0:
        i = order[0]
        keep.append(preds[i])
	
        cur_box = preds[i, :4]
        rest_boxes = preds[order[1:], :4]

        iou = cal_iou(cur_box, rest_boxes)

        inds = np.where(iou <= iou_thr)[0]
        order = order[inds + 1]

    return np.array(keep)
```