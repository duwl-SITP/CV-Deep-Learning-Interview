---
questions: 1
source: "[[me]]"
---
## 1. RoPE旋转位置编码

带有旋转位置编码的位置 $m$ 的 $Query$ 和 位置 $n$ 的 $Key$ :
$$ Q^{'}K^{'T} $$
以$R(\cdot)$表示RoPE，带入上式并展开转置得：
$$ QR(m)(KR(n))^{T} = QR(m)R^{T}(n)K^{T} $$
RoPE计算公式为：
$$ RoPE(\theta) = 
\begin{bmatrix}
cos(\theta)&-sin(\theta)\\
sin(\theta)&cos(\theta)
\end{bmatrix}
$$
$R^{T}$有性质：
$$ R^{T}(n) = \begin{bmatrix}
cos(\theta)&sin(\theta)\\
-sin(\theta)&cos(\theta)
\end{bmatrix} = 
R(-n)
$$
带入$R(m)R(-n)$:
$$
\begin{align}
\begin{bmatrix}
cos(m\theta) & -sin(m\theta)\\
sin(m\theta) & cos(m\theta)
\end{bmatrix} 
\begin{bmatrix}
cos(-n\theta) & -sin(-n\theta)\\
sin(-n\theta) & cos(-n\theta)
\end{bmatrix} &= 

\begin{bmatrix}
cos(m\theta)cos(n\theta)+sin(m\theta)sin(n\theta) & -cos(m\theta)sin(n\theta)+sin(m\theta)cos(n\theta) \\
sin(m\theta)cos(n\theta)-cos(m\theta)sin(n\theta) & cos(m\theta)cos(n\theta) + sin(m\theta)sin(n\theta)
\end{bmatrix} \\

&= 
\begin{bmatrix}
cos((m-n)\theta) & -sin((m-n)\theta)\\
sin((m-n)\theta) & cos((m-n)\theta)
\end{bmatrix} \\

&=
RoPE((m-n)\theta)
\end{align}
$$

代入$Q^{'}K^{'T}$，得：
$$QR(m-n)K^{T}$$
### 对比传统正余弦编码

正余弦是：
$$  
(Q+PE_i)​)(K+PE_j​)^{T}
$$
问题：
- 位置信息是“混进去”的
- 模型要自己学出相对关系

